// Create a new JavaScript object with some properties
const testObj = { 
    name: 'test',
    value: 'This is a test object'
}; 

// Check if the object has been created correctly 
if (testObj.name === 'test' && testObj.value === 'This is a test object') {
    console.log('The test object has been created successfully!');
}
